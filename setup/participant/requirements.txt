# Vector Search ハンズオン 必要パッケージ
# 実際に使用されるパッケージのみを記載

# === コアパッケージ ===

# Milvus クライアント
pymilvus==2.3.4

# 埋め込みモデル (Sentence Transformers)
sentence-transformers==3.0.1

# Web Framework (FastAPI)
fastapi==0.109.0
uvicorn[standard]==0.27.0

# 環境変数管理
python-dotenv==1.0.0

# === 依存関係（自動インストールされるが明示） ===

# PyMilvus の依存関係
# grpcio>=1.60.0  # pymilvusが必要とするバージョンを自動インストール

# Sentence Transformers の依存関係
transformers>=4.41.0
torch>=2.3.0
huggingface-hub>=0.23.0

# FastAPI の依存関係
pydantic>=2.5.0
starlette>=0.35.0
marshmallow<4.0.0  # pydanticの依存関係として必要、4.0.0以降は互換性問題あり

# scikit-learn (test_embeddings_hf.pyで使用)
scikit-learn>=1.5.0

# === 注意事項 ===
# - バージョンは>=で指定し、互換性を保ちつつ最新版を使用可能にする
# - 依存関係は各パッケージが自動的に解決するため、細かいバージョン固定は不要
# - NumPyなどの基本パッケージは依存関係として自動インストールされる