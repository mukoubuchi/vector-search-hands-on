# Vector Search Hands-on Required Packages
# Only packages actually used are listed.
# Python 3.11-3.14 is required (ibm-watsonx-ai declares >=3.11,<3.15).

# === Core Packages ===

# IBM watsonx.ai client (embeddings)
ibm-watsonx-ai==1.7.2

# OpenSearch client (k-NN vector search and BM25 keyword search)
opensearch-py==3.2.0

# Web Framework (FastAPI)
fastapi==0.141.1
uvicorn[standard]==0.53.0

# Environment variable management
python-dotenv==1.2.3

# === Pinned dependencies (auto-installed; pinned so every participant gets the same versions) ===

# FastAPI dependency
pydantic==2.13.5
