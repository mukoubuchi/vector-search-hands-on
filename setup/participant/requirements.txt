# Vector Search Hands-on Required Packages
# Only packages actually used are listed

# === Core Packages ===

# Milvus client (kept on 2.3.x to match the Milvus 2.3.3 server)
pymilvus==2.3.4

# Embedding model (Sentence Transformers)
sentence-transformers==5.5.1

# Web Framework (FastAPI)
fastapi==0.136.3
uvicorn[standard]==0.49.0

# Environment variable management
python-dotenv==1.2.2

# === Dependencies (auto-installed, listed for clarity) ===

# Sentence Transformers dependencies
transformers>=4.41.0
torch>=2.3.0
huggingface-hub>=0.23.0

# FastAPI dependencies
pydantic>=2.7.0
marshmallow<4.0.0  # required by pymilvus/environs; 4.0.0+ has compatibility issues

# scikit-learn (used in test_embeddings_hf.py)
scikit-learn>=1.5.0
