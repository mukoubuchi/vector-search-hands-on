(function() {
    window.VectorSearchHandsOn = window.VectorSearchHandsOn || {};

    window.VectorSearchHandsOn.site = {
        projectBase: '/vector-search-hands-on',
        navPaths: [
            '/',
            '/preparation/',
            '/part1/',
            '/part2/',
            '/part3/',
            '/summary/',
            '/feedback/'
        ],
        languages: ['en', 'ja']
    };
})();
