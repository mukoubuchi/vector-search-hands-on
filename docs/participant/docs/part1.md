# Part 1: Vector Searchを体験しよう

**所要時間**: 15分

このパートでは、Vector Search（ベクトル検索）がどのように動作するかを実際に体験します。

## このパートのゴール

- Vector Searchとは何かを理解する
- 実際にVector Searchを動かしてみる
- 「意味で検索」の便利さを体感する

## ステップ1: Vector Searchとは？（5分）

### 1-1. 従来の検索の問題点

まず、普段使っている検索の問題点を理解しましょう。

#### 例: ECサイトで商品を探す場合

**あなたの検索**: 「赤いスニーカー」

**従来の検索結果**:
- 「赤いスニーカー」→ 見つかる
- 「赤色のランニングシューズ」→ 見つからない
- 「レッドのスポーツシューズ」→ 見つからない

**なぜ見つからない？**
- 従来の検索は「文字」を探すだけ
- 「赤い」と「赤色」は違う文字として扱われる
- 「スニーカー」と「ランニングシューズ」も違う文字

### 1-2. Vector Searchの仕組み

Vector Searchは「意味」を理解して検索します。

#### 同じ例でVector Searchを使うと

**あなたの検索**: 「赤いスニーカー」

**Vector Searchの結果**:
- 「赤いスニーカー」→ 見つかる
- 「赤色のランニングシューズ」→ 見つかる（意味が似ている）
- 「レッドのスポーツシューズ」→ 見つかる（意味が似ている）

**なぜ見つかる？**
- Vector Searchは「意味」を理解する
- 「赤い」「赤色」「レッド」→ 同じ意味と理解
- 「スニーカー」「ランニングシューズ」「スポーツシューズ」→ 似た意味と理解

### 1-3. Vector Searchの仕組み（簡単に）

Vector Searchは以下の手順で動作します：

```
ステップ1: テキストを数値に変換
「赤いスニーカー」→ [0.2, 0.8, 0.1, 0.5, ...] （ベクトル）

ステップ2: 似た数値を探す
データベースから似た数値のパターンを検索

ステップ3: 結果を返す
似た意味の商品を返す
```

**ポイント**:
- 「ベクトル」= 数値の配列（例: [0.2, 0.8, 0.1, ...]）
- 意味が似ていると、数値のパターンも似る
- コンピュータは数値の類似度を高速に計算できる

### 1-4. 実際の活用例

Vector Searchは様々な場面で使われています：

#### ECサイト
- 「この商品に似た商品を探す」
- 「初心者向けのカメラ」→ 入門用カメラを提案

#### 社内検索
- 「この資料に似た文書を探す」
- 「プロジェクト計画書」→ 似た計画書を検索

#### カスタマーサポート
- 「似た質問を探す」
- 過去の回答を素早く見つける

## ステップ2: 接続テストを実行（5分）

!!! example "実践: ここから手を動かします"
    実際にVector Searchを動かす前に、必要なサービスに接続できるか確認します。

### 2-1. ターミナルを開く

1. **IBM Bob IDEの画面下部を確認**
   - 「TERMINAL」というタブがあります

2. **TERMINALタブをクリック**
   - 黒い画面（ターミナル）が表示されます

3. **ターミナルとは？**
   - コンピュータに命令を出す画面
   - 文字で命令を入力します

### 2-2. setupフォルダに移動

ターミナルに以下を入力してEnterキーを押します：

```bash
cd setup
```

**説明**:
- `cd` = "Change Directory"（フォルダを移動する命令）
- `setup` = 移動先のフォルダパス

**確認**:
- ターミナルの表示が変わります
- `~/vector-search-handson/setup` のような表示になればOK

### 2-3. 接続テストを実行

ターミナルに以下を入力してEnterキーを押します：

```bash
python test_connection_simple.py
```

**説明**:
- `python` = Pythonプログラムを実行する命令
- `test_connection_simple.py` = 接続テストのプログラム

### 2-4. 結果を確認

接続テストが実行されます。以下のような結果が表示されます：

#### :fontawesome-solid-check: 成功の場合

```
==================================================
Milvus & 埋め込みモデル 接続テスト
==================================================

=== 環境変数確認 ===
✓ MILVUS_HOST: 192.168.1.100  ← 講師から配布されたIPアドレス
✓ MILVUS_PORT: 19530
✓ EMBEDDING_MODEL: paraphrase-multilingual-MiniLM-L12-v2
✓ EMBEDDING_DIMENSION: 384

=== Milvus 接続テスト ===
接続先: 192.168.1.100:19530
✓ Milvusに接続成功
✓ 既存コレクション数: 0

=== 埋め込みモデル テスト ===
モデル: paraphrase-multilingual-MiniLM-L12-v2
モデルをロード中...
✓ モデルのロードに成功しました

埋め込みを生成中...
✓ 埋め込みベクトル生成成功
  次元数: 384
  最初の5要素: [-0.047, 0.042, -0.003, 0.028, 0.047]

==================================================
テスト結果サマリー
==================================================
Milvus接続:        ✓ 成功
埋め込みモデル:    ✓ 成功

✓ すべての接続テストが成功しました！
```

**これは何？**:
- **Milvus**: ベクトルデータベース（データを保存する場所）
- **埋め込みモデル**: Hugging Face Transformers（テキストをベクトルに変換）
- **埋め込みベクトル**: テキストを数値に変換したもの
- **次元数 384**: 384個の数値で意味を表現

#### :fontawesome-solid-xmark: 失敗の場合

```
✗ Milvus接続エラー: Connection refused
```

**対処法**:
1. 講師に声をかける
2. 接続情報（`.env`ファイル）を確認
3. インターネット接続を確認

## ステップ3: Vector Searchを体験（5分）

!!! example "実践: Vector Searchを動かしてみよう"
    接続テストが成功したら、実際にVector Searchを体験しましょう。

### 3-1. デモアプリケーションを起動

#### 方法1: IBM Bobに依頼（推奨）

1. **IBM Bobのチャット画面を開く**
   - 画面下部のチャット入力欄をクリック

2. **以下を入力してEnter**
   ```
   setupフォルダでデモアプリケーションを起動して
   ```

3. **IBM Bobが自動的に起動**
   - ターミナルでコマンドが実行されます
   - アプリケーションが起動します

#### 方法2: 手動で起動

ターミナルに以下を入力してEnter：

```bash
python app.py
```

### 3-2. 起動を確認

ターミナルに以下のような表示が出ればOK：

```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://localhost:8000
```

**重要**:
- この画面は閉じないでください
- アプリケーションが動いている間、ずっと表示されます

### 3-3. Swagger UIを開く

1. **Webブラウザを開く**
   - Chrome、Firefox、Safari、Edgeなど

2. **以下のURLにアクセス**
   ```
   http://localhost:8000/docs
   ```

3. **Swagger UIが表示される**
   - APIのドキュメントが表示されます
   - 「Swagger UI」= APIをテストできる画面

### 3-4. Swagger UIとは？

**Swagger UI** は、APIを視覚的にテストできるツールです。

**できること**:
- APIの一覧を見る
- APIを実際に実行する
- 結果をすぐに確認する

**メリット**:
- コードを書かずにテストできる
- 結果がすぐに分かる
- 初心者でも使いやすい

### 3-5. 検索を試してみる

#### ステップ1: /searchエンドポイントを開く

1. **Swagger UI画面で「/search」を探す**
   - 緑色の「POST」ボタンの横に「/search」と書いてあります

2. **「/search」をクリック**
   - エンドポイントの詳細が展開されます

#### ステップ2: 「Try it out」をクリック

1. **右上の「Try it out」ボタンをクリック**
   - 入力フィールドが編集可能になります

#### ステップ3: 検索クエリを入力

1. **「Request body」の欄に以下を入力**
   ```json
   {
     "query": "赤いスニーカー"
   }
   ```

2. **入力内容の説明**
   - `query`: 検索したい言葉
   - `"赤いスニーカー"`: 探したい商品

#### ステップ4: 「Execute」をクリック

1. **青い「Execute」ボタンをクリック**
   - 検索が実行されます

2. **少し待つ**
   - 数秒で結果が表示されます

#### ステップ5: 結果を確認

画面を下にスクロールすると、以下のような結果が表示されます：

```json
{
  "results": [
    {
      "product_name": "赤いランニングシューズ",
      "similarity_score": 0.92,
      "price": 8900,
      "category": "スニーカー",
      "description": "軽量で通気性の良いランニングシューズ"
    },
    {
      "product_name": "レッドスポーツシューズ",
      "similarity_score": 0.88,
      "price": 7500,
      "category": "スニーカー",
      "description": "カジュアルにも使えるスポーツシューズ"
    }
  ]
}
```

**結果の見方**:
- `product_name`: 商品名
- `similarity_score`: 類似度（0.0〜1.0、高いほど似ている）
- `price`: 価格
- `category`: カテゴリ
- `description`: 説明

### 3-6. 色々な検索を試してみる

以下のような検索を試してみましょう：

#### 例1: 初心者向けの商品を探す

```json
{
  "query": "初心者向けのカメラ"
}
```

**期待される結果**:
- 「入門用デジタルカメラ」
- 「ビギナー向けカメラ」
- 「簡単操作のカメラ」

#### 例2: ビジネス向けの商品を探す

```json
{
  "query": "ビジネス向けのノートパソコン"
}
```

**期待される結果**:
- 「ビジネスノートPC」
- 「オフィス用ノートパソコン」
- 「仕事用ノートPC」

#### 例3: 高性能な商品を探す

```json
{
  "query": "高性能なゲーミングPC"
}
```

**期待される結果**:
- 「ハイスペックゲーミングPC」
- 「高速ゲーミングコンピュータ」
- 「パワフルなゲーム用PC」

### 3-7. Vector Searchの凄さを実感

色々な検索を試すと、以下のことに気づくはずです：

**気づき1: 言い方が違っても見つかる**
- 「初心者向け」→「入門用」「ビギナー向け」も見つかる
- 「高性能」→「ハイスペック」「高速」も見つかる

**気づき2: 類似度スコアが便利**
- スコアが高い = より似ている
- スコアが低い = あまり似ていない
- 結果の信頼度が分かる

**気づき3: 説明文も考慮される**
- 商品名だけでなく、説明文の意味も理解
- より正確な検索結果

## :fontawesome-solid-check: Part 1 完了チェック

以下を確認してください：

- [ ] Vector Searchとは何かを理解した
- [ ] 従来の検索との違いを理解した
- [ ] 接続テストが成功した
- [ ] デモアプリケーションを起動できた
- [ ] Swagger UIを開けた
- [ ] 検索を実行できた
- [ ] 検索結果を確認できた
- [ ] 色々な検索を試した

## Part 1で学んだこと

### 理解したこと

- :fontawesome-solid-check: Vector Searchは「意味」を理解して検索する
- :fontawesome-solid-check: 従来の検索は「文字」を探すだけ
- :fontawesome-solid-check: ベクトル = テキストを数値に変換したもの
- :fontawesome-solid-check: 類似度スコア = どれくらい似ているかの指標

### できるようになったこと

- :fontawesome-solid-check: 接続テストの実行
- :fontawesome-solid-check: デモアプリケーションの起動
- :fontawesome-solid-check: Swagger UIの使い方
- :fontawesome-solid-check: Vector Searchの実行

## 次のステップ

Part 1が完了したら、[Part 2: IBM Bobで機能を追加](part2.md)に進みましょう！

Part 2では、IBM Bobを使って、このアプリケーションに新しい機能を追加します。

**Part 2で追加する機能**:
- 検索結果に商品画像を表示
- 価格でフィルター
- レコメンド理由を表示

プログラミング経験がなくても大丈夫です。IBM Bobが全部やってくれます！

## :fontawesome-solid-circle-question: 困ったときは

### よくある質問

#### Q1: 接続テストが失敗する

**対処法**:
1. `.env`ファイルの内容を確認
2. インターネット接続を確認
3. 講師に相談

#### Q2: Swagger UIが開けない

**対処法**:
1. アプリケーションが起動しているか確認
2. URLが正しいか確認（`http://localhost:8000/docs`）
3. ブラウザを変えてみる
4. 講師に相談

#### Q3: 検索結果が0件

**対処法**:
1. サンプルデータが投入されているか確認
2. 検索クエリを変えてみる
3. 講師に相談

#### Q4: エラーメッセージが表示される

**対処法**:
1. エラーメッセージをコピー
2. 講師に見せる
3. 一緒に解決

---

**お疲れ様でした！Part 1が完了しました！**

休憩を取ってから、Part 2に進みましょう！
